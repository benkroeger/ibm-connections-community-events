'use strict';

// node core modules

// 3rd party modules

// internal modules
const attendees = require('./attendees');
const events = require('./events');
const event = require('./event');

module.exports = {
  attendees,
  events,
  event,
};
